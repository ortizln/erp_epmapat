spring:
    application:
        name: mc_gateway
    cloud:
        config:
            enabled: true
            uri: http://localhost:8081
    jpa:
        database: POSTGRESQL
        show-sql: false
    datasource:
        url: jdbc:postgresql://localhost:5432/ErpEpmapaT
        username: postgres
        password: 12345
