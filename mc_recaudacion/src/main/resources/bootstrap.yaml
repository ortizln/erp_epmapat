spring:
  application:
    name: mc_recaudacion
  cloud:
    config:
      enabled: true
      uri: http://config-service:8081
  jpa:
    database: POSTGRESQL
    show-sql: false
  datasource:
    url: ${DB}
    username: postgres
    password: 086411421
