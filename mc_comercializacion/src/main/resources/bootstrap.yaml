spring:
    application:
        name: mc_comercializacion
    cloud:
        config:
            enabled: true
            uri: http://localhost:8080
        loadbalancer:
            hint:
                mc_comercializacion: custom
    jpa:
        database: POSTGRESQL
        show-sql: false
    datasource:
        url: jdbc:postgresql://localhost:5432/ErpEpmapaT
        username: postgres
        password: 12345
