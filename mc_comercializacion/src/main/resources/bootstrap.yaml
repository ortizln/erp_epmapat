spring:
  application:
    name: mc_comercializacion
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: none # Recommended for production
  datasource:
    url: jdbc:postgresql://localhost:5432/erp_epmapat
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
